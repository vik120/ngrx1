import { SplicetextPipe } from './splicetext.pipe';

describe('SplicetextPipe', () => {
  it('create an instance', () => {
    const pipe = new SplicetextPipe();
    expect(pipe).toBeTruthy();
  });
});
